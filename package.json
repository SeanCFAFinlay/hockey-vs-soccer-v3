{
  "name": "hockey-vs-soccer-v3",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:editor": "npm -w apps/editor run dev",
    "dev:game": "npm -w apps/game run dev",
    "build": "npm -ws run build",
    "lint": "npm -ws run lint",
    "typecheck": "npm -ws run typecheck",
    "test": "echo \"No tests configured yet\" && exit 0"
  },
  "devDependencies": {
    "eslint": "^8.57.1",
    "typescript": "^5.4.5"
  },
  "overrides": {
    "esbuild": "^0.25.0"
  }
}
